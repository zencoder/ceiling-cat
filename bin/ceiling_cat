#!/usr/bin/env ruby
require 'rubygems'
require 'ceiling_cat'

file = ARGV[0] || './Chatfile'

if File.exists?(file)
  load file
  CeilingCat::Setup.new.connect
else
  puts "Couldn't find a Chatfile at #{file}! Create one at `./Chatfile` or provide a path to one."
end